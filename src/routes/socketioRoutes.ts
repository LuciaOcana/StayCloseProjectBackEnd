import express from "express";
//import { connectedUsers } from "../utils/socket";


const router = express.Router();

/*
router.get("/connected-users", (_req, res) => {
  res.json(connectedUsers.map(({username}) => ({username})));
});
*/


export default router;
